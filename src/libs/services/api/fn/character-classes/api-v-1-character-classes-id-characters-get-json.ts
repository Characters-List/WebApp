/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { CharacterDto } from '../../models/character-dto';

export interface ApiV1CharacterClassesIdCharactersGet$Json$Params {
  id: string;
}

export function apiV1CharacterClassesIdCharactersGet$Json(http: HttpClient, rootUrl: string, params: ApiV1CharacterClassesIdCharactersGet$Json$Params, context?: HttpContext): Observable<StrictHttpResponse<Array<CharacterDto>>> {
  const rb = new RequestBuilder(rootUrl, apiV1CharacterClassesIdCharactersGet$Json.PATH, 'get');
  if (params) {
    rb.path('id', params.id, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'text/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<Array<CharacterDto>>;
    })
  );
}

apiV1CharacterClassesIdCharactersGet$Json.PATH = '/api/v1/CharacterClasses/{id}/Characters';
